# Windows GPU test workflow (self-hosted) - called by test-matrix.yml
name: Test Windows GPU

on:
  workflow_call:
    inputs:
      config-file:
        type: string
        default: "comfy-test.toml"
      timeout-minutes:
        type: number
        default: 60

jobs:
  windows-gpu-test:
    name: windows-gpu-test
    runs-on: ["self-hosted", "windows", "gpu"]
    timeout-minutes: ${{ inputs.timeout-minutes }}
    env:
      PYTHONUNBUFFERED: "1"
      COMFY_TEST_GPU: "1"
      COMFY_TEST_LOGS_DIR: ${{ github.workspace }}\comfy-test-logs
      COMFY_TEST_WORKSPACE_DIR: ${{ github.workspace }}\test_workspaces
      NODE_NAME: ${{ github.event.repository.name }}
      COMFYUI_DIR: ${{ github.workspace }}\ComfyUI
      PYTHON_DIR: ${{ github.workspace }}\python
      PYTHON_EXE: ${{ github.workspace }}\python\python.exe
    steps:
      - name: Set cache directory
        shell: powershell
        run: echo "LOCAL_CACHE_DIR=$env:RUNNER_TOOL_CACHE\comfy-test-cache\windows-gpu-v2" >> $env:GITHUB_ENV

      # --- Restore local cache ---
      - name: Restore local cache
        id: local-cache
        shell: powershell
        run: |
          if (Test-Path "$env:LOCAL_CACHE_DIR\python") {
            Write-Host "Restoring from local cache..."
            robocopy "$env:LOCAL_CACHE_DIR\python" "$env:PYTHON_DIR" /E /MT /NFL /NDL /NJH /NJS
            robocopy "$env:LOCAL_CACHE_DIR\ComfyUI" "$env:COMFYUI_DIR" /E /MT /NFL /NDL /NJH /NJS
            Write-Host "Cache restored!"
            echo "cache-hit=true" >> $env:GITHUB_OUTPUT
          } else {
            Write-Host "No local cache found"
            echo "cache-hit=false" >> $env:GITHUB_OUTPUT
          }
          exit 0

      # --- Setup Python (skip if cache hit) ---
      - name: Setup Python
        if: steps.local-cache.outputs.cache-hit != 'true'
        shell: powershell
        run: |
          Write-Host "Downloading Python embeddable..."
          $pythonUrl = "https://www.python.org/ftp/python/3.13.0/python-3.13.0-embed-amd64.zip"
          Invoke-WebRequest -Uri $pythonUrl -OutFile python.zip
          Expand-Archive python.zip -DestinationPath $env:PYTHON_DIR
          Remove-Item python.zip

          # Enable pip (uncomment import site in ._pth file)
          $pthFile = Get-ChildItem "$env:PYTHON_DIR\*.pth" | Select-Object -First 1
          (Get-Content $pthFile) -replace '#import site','import site' | Set-Content $pthFile

          # Install pip
          Write-Host "Installing pip..."
          Invoke-WebRequest -Uri "https://bootstrap.pypa.io/get-pip.py" -OutFile get-pip.py
          & $env:PYTHON_EXE get-pip.py --no-warn-script-location
          Remove-Item get-pip.py

          # Install CUDA torch
          Write-Host "Installing CUDA torch..."
          & $env:PYTHON_EXE -m pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu130 --no-warn-script-location

          # Clone ComfyUI and install requirements
          Write-Host "Cloning ComfyUI..."
          git clone --depth 1 https://github.com/comfyanonymous/ComfyUI.git $env:COMFYUI_DIR

          # Patch requirements.txt to remove torch (already installed with CUDA)
          (Get-Content "$env:COMFYUI_DIR\requirements.txt") | Where-Object { $_ -notmatch '^(torch|torchvision|torchaudio)$' } | Set-Content "$env:COMFYUI_DIR\requirements.txt"

          Write-Host "Installing ComfyUI requirements..."
          & $env:PYTHON_EXE -m pip install -r "$env:COMFYUI_DIR\requirements.txt" --no-warn-script-location

          # Install playwright
          Write-Host "Installing playwright..."
          & $env:PYTHON_EXE -m pip install playwright --no-warn-script-location
          & $env:PYTHON_EXE -m playwright install chromium

      # --- Save local cache ---
      - name: Save local cache
        if: steps.local-cache.outputs.cache-hit != 'true'
        shell: powershell
        run: |
          Write-Host "Saving to local cache..."
          New-Item -ItemType Directory -Force -Path "$env:LOCAL_CACHE_DIR" | Out-Null
          robocopy "$env:PYTHON_DIR" "$env:LOCAL_CACHE_DIR\python" /E /MT /NFL /NDL /NJH /NJS
          robocopy "$env:COMFYUI_DIR" "$env:LOCAL_CACHE_DIR\ComfyUI" /E /MT /NFL /NDL /NJH /NJS
          Write-Host "Cache saved!"
          exit 0

      # --- Verify CUDA torch ---
      - name: Verify CUDA torch
        shell: powershell
        run: |
          Write-Host "Verifying CUDA torch installation..."
          & $env:PYTHON_EXE -c @"
          import torch
          print('PyTorch:', torch.__version__)
          print('CUDA available:', torch.cuda.is_available())
          print('CUDA version:', torch.version.cuda)
          if torch.cuda.is_available():
              print('Device:', torch.cuda.get_device_name(0))
          "@

      # --- Install comfy-test (fresh each run) ---
      - name: Install comfy-test
        shell: powershell
        run: & $env:PYTHON_EXE -m pip install comfy-test --no-warn-script-location

      # --- Checkout and run ---
      - uses: actions/checkout@v4
        with:
          path: ${{ env.COMFYUI_DIR }}\custom_nodes\${{ github.event.repository.name }}

      - name: Install node dependencies
        shell: powershell
        working-directory: ${{ env.COMFYUI_DIR }}\custom_nodes\${{ env.NODE_NAME }}
        run: |
          if (Test-Path "requirements.txt") { & $env:PYTHON_EXE -m pip install -r requirements.txt --no-warn-script-location }
          if (Test-Path "install.py") { & $env:PYTHON_EXE install.py }

      - name: Install validate endpoint
        shell: powershell
        run: |
          git clone --depth 1 https://github.com/PozzettiAndrea/ComfyUI-validate-endpoint.git "$env:COMFYUI_DIR\custom_nodes\ComfyUI-validate-endpoint"
          $true

      - name: Run tests
        shell: powershell
        working-directory: ${{ env.COMFYUI_DIR }}\custom_nodes\${{ env.NODE_NAME }}
        run: |
          $serverLog = "$env:COMFY_TEST_LOGS_DIR\server.log"
          New-Item -ItemType Directory -Force -Path $env:COMFY_TEST_LOGS_DIR | Out-Null

          Write-Host "Starting GPU server (logging to $serverLog)..."
          $env:COMFY_ENV_DEBUG = "1"
          $serverProcess = Start-Process -FilePath $env:PYTHON_EXE `
            -ArgumentList "-u", "$env:COMFYUI_DIR\main.py", "--listen", "127.0.0.1", "--port", "8188" `
            -NoNewWindow -PassThru -RedirectStandardOutput $serverLog -RedirectStandardError "$env:COMFY_TEST_LOGS_DIR\server_stderr.log"

          Write-Host "Waiting for server (up to 5 minutes)..."
          $serverReady = $false
          for ($i = 1; $i -le 300; $i++) {
            if ($serverProcess.HasExited) {
              Write-Host "Server process died! Exit code: $($serverProcess.ExitCode)"
              Write-Host "=== Server stdout ==="
              Get-Content $serverLog -ErrorAction SilentlyContinue
              Write-Host "=== Server stderr ==="
              Get-Content "$env:COMFY_TEST_LOGS_DIR\server_stderr.log" -ErrorAction SilentlyContinue
              exit 1
            }
            try {
              Invoke-WebRequest -Uri "http://127.0.0.1:8188/system_stats" -UseBasicParsing -ErrorAction Stop | Out-Null
              Write-Host "Server is ready after $i seconds!"
              $serverReady = $true
              break
            } catch { Start-Sleep -Seconds 1 }
          }

          if (-not $serverReady) {
            Write-Host "Server failed to start within 5 minutes"
            Write-Host "=== Server stdout ==="
            Get-Content $serverLog -ErrorAction SilentlyContinue
            Write-Host "=== Server stderr ==="
            Get-Content "$env:COMFY_TEST_LOGS_DIR\server_stderr.log" -ErrorAction SilentlyContinue
            Stop-Process -Id $serverProcess.Id -Force -ErrorAction SilentlyContinue
            exit 1
          }

          & "$env:PYTHON_DIR\Scripts\comfy-test.exe" run --server-url http://127.0.0.1:8188
          $testExitCode = $LASTEXITCODE

          Stop-Process -Id $serverProcess.Id -Force -ErrorAction SilentlyContinue
          exit $testExitCode

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: results-windows-gpu
          path: |
            ${{ env.COMFY_TEST_LOGS_DIR }}/
          if-no-files-found: ignore
          include-hidden-files: true

      # --- Cleanup ---
      - name: Cleanup workspace
        if: always()
        shell: powershell
        run: |
          Remove-Item -Path "$env:GITHUB_WORKSPACE\*" -Recurse -Force -ErrorAction SilentlyContinue
          Remove-Item -Path "$env:GITHUB_WORKSPACE\.*" -Recurse -Force -ErrorAction SilentlyContinue
